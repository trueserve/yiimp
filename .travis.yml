os:
    - linux

dist:
    - trusty

language:
    - cpp

sudo:
    - true

branches:
    only:
        - MergeAllTheThings

script:
    - (cd blocknotify && make)
    - (cd stratum/iniparser && make)
    - (cd stratum && make)
